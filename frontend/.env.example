# --- Kickr frontend (build.md §10) ---

# Backend origin (FastAPI). No trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Privy app id. Leave blank to use the dev sign-in (pick a handle, get 1,000 chips).
NEXT_PUBLIC_PRIVY_APP_ID=
